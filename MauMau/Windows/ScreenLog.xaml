<Window x:Class="MauMau.Windows.ScreenLog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MauMau.Windows"
        mc:Ignorable="d"
        WindowStyle="SingleBorderWindow"
        Title="Log de operações" Height="358.131" Width="287.543" KeyDown="Window_KeyDown">
    <Grid>
        <ListBox x:Name="logfield" HorizontalAlignment="Left" Height="296" VerticalAlignment="Top" Width="280" Margin="0,31,0,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button x:Name="btnlogclear" Content="Limpar log" HorizontalAlignment="Left" Margin="195,6,0,0" VerticalAlignment="Top" Width="75" Cursor="Hand" Click="btnlogclear_Click" />

    </Grid>
</Window>
